# T6 Brief: Blog Ch10 + 08_art_grpo.py

## 背景

我们在为已完成的研究项目 **agentic-gov**（位于 `/Users/sunxichen/Projects/agentic-gov`）撰写面试复现材料。项目是政务（公积金）task-oriented agent 的 SFT→RL 全链路研究，共 6 个 phase（sandbox 环境/任务工厂 → SFT 数据合成+过滤 → SFT 训练 → user simulator → release gate → ART GRPO RL）。RL 基于 OpenPipe ART 框架（源码 `/Users/sunxichen/Projects/ART`，官方 upstream，约 0.5.17 语义；项目使用其 LocalBackend：unsloth 训练 + vLLM serving），项目侧 RL 代码在 `phase6/`。

最终交付物是两份同等地位、相互独立的材料，供 20-30 分钟面试自述：

1. **recap blog**：`/Users/sunxichen/Projects/leetcode-practice-app/leetcode-practice-web/.scratch/interview-deck/agentic-gov-recap/recap-blog.md`（单文件长文，多个 writer 分章撰写）
2. **recap code**：同目录 `recap-code/` 下的 8 个伪代码文件

你负责的是**全文技术含量最高的一章**：把 ART 这个"黑盒"拆开讲清楚。读者面 RL 算法岗时，这章是被深挖的主战场。

## 先读

- 同目录 `spec-recap-blog.md` — 完整规格
- 同目录 `tickets.md` — 你的 ticket 是 T6，验收标准以它为准
- 同目录 `fact-base.md`（如已存在）— 事实底稿，**只是参考起点，不是唯一来源；你必须自己通读项目与 ART 源码核实细节**
- 建议通读：`research-proposal/art-framework-deep-dive.md`、`research-proposal/ART框架学习.md`、`phase6/art/train_grpo.py`、ART 的 `src/art/loss.py`、`gather.py`、`model.py`、`local/backend.py`

## 你负责的部分

**Blog 章节**（写到分片文件，见协作约定）：

- **Ch10 ART GRPO 训练**，两层结构：
  1. **项目侧编排**：`phase6/art/train_grpo.py` 的训练循环——任务采样 → rollout → adjudicator 打分 → gather → log → train，每步在做什么、为什么这么编排
  2. **ART 黑盒拆解**：`gather_trajectory_groups` 的分组/等待语义、`TrainableModel.log` 的 trajectory→batch 转换、`_train_step` 里的 GRPO loss（importance ratio、PPO clip、优势归一化、KL penalty）、loss 归一化与长度偏置（结合项目 `phase6/art/loss_norm_floor.py` 的实证）、vLLM 权重同步 + LoRA serving 机制
- 两个决策插叙：⑨KL penalty 的设计与解读（出处 docs/experiment-notes/020）；⑧两个工程发现——(a) 排查训练耗时时发现把 rollout 用的 LoRA merge 进 base model 后 serving 变快（出处 docs/experiment-notes/023、handoff lora-serving 相关）；(b) async RL：异步 rollout 带来的 off-policy drift、CISPO vs GRPO 的取舍、merged serving（出处 024、025 及相关 handoff）。async RL 是业界重要方向，要结合项目实证讲透（但不做通用科普）
- 配一张 mermaid 组件交互图（trainer / vLLM serving / sim server / adjudicator 之间）

**代码文件**（写入 `recap-code/`）：

- `08_art_grpo.py` — 分两层：上半部分项目侧编排伪代码，下半部分 ART 内部机制伪代码

## 代码写法约定

- 语法合法的 Python，可读但不可运行；保留真实 import 路径、函数签名、类型标注、控制流骨架；省略处用 `...` 或一行注释带过
- **函数名必须使用真实函数名**：项目侧来自 `phase6/art/`，ART 侧来自 `/Users/sunxichen/Projects/ART/src/art/`（逐一核实存在）
- 中文注释，解释函数**内部执行逻辑**和为什么这么做——比如 GRPO loss 里 importance ratio 怎么算、clip 在哪、KL 项怎么进 loss
- 文件顶部 docstring 说明该阶段在全链路中的位置

## 写作风格（重要）

- 中文，技术术语保留英文原词
- **不要黑话、不要营销腔**。像一个资深算法工程师在梳理自己的项目、向其他工程师介绍。类似 All-in-x / Hands-on-x 技术 blog 的平实风格
- 决策插叙要讲清楚：当时遇到什么问题 → 有哪些选项 → 为什么选了这个 → 结果如何
- RL 算法点（GRPO vs PPO、importance ratio、优势归一化、KL、CISPO）只在项目真实用到的语境里解释，标准是"面试被追问时这章的内容能接住"

## 协作约定（多个 writer 并行写同一个 blog 文件）

- 你只写 Ch10。把内容写到 `recap-blog.d/ch10.md` 分片文件（目录不存在就自己建），**不要直接写 recap-blog.md**，最后由 orchestrator 合并
- 章节标题格式：`## Ch10 ART GRPO 训练：...`
- Ch8-9（RL 数据采样、rollout/reward）由另一个 writer 负责，衔接处一句话带过

## 约束

- 只读 agentic-gov 和 ART，不修改它们的任何文件
- 写入位置仅限 `.scratch/interview-deck/agentic-gov-recap/` 下的 `recap-blog.d/ch10.md`、`recap-code/08_art_grpo.py`
- 完成后回复：产出文件清单、章节小节结构、你引用的关键源码文件（项目侧与 ART 侧分开列）、你认为需要 orchestrator 注意的问题
