# T3 Brief: Blog Ch3-4 + 03_sft_synthesis.py + 04_sft_filtering.py

## 背景

我们在为已完成的研究项目 **agentic-gov**（位于 `/Users/sunxichen/Projects/agentic-gov`）撰写面试复现材料。项目是政务（公积金）task-oriented agent 的 SFT→RL 全链路研究，共 6 个 phase（sandbox 环境/任务工厂 → SFT 数据合成+过滤 → SFT 训练 → user simulator → release gate → ART GRPO RL）。RL 基于 OpenPipe ART 框架（源码 `/Users/sunxichen/Projects/ART`）。

最终交付物是两份同等地位、相互独立的材料，供 20-30 分钟面试自述：

1. **recap blog**：`/Users/sunxichen/Projects/leetcode-practice-app/leetcode-practice-web/.scratch/interview-deck/agentic-gov-recap/recap-blog.md`（单文件长文，多个 writer 分章撰写）
2. **recap code**：同目录 `recap-code/` 下的 8 个伪代码文件

## 先读

- 同目录 `spec-recap-blog.md` — 完整规格（你的章节大纲在里面）
- 同目录 `tickets.md` — 你的 ticket 是 T3，验收标准以它为准
- 同目录 `fact-base.md`（如已存在）— 事实底稿，**只是参考起点，不是唯一来源；你必须自己通读项目相关源码核实细节**

## 你负责的部分

**Blog 章节**（写到分片文件，见协作约定）：

- **Ch3 SFT 数据合成**（Phase2 核心）：agent/user teacher 双角色、prompt 模板版本化（v1.0 frozen / v1.1 current）、`<analysis>/<action>` envelope 格式、parser 严格 XML 解析、orchestrator 的 current-turn repair（action-only 修复 + parse_feedback 重试）与 semantic guard（重复写操作防护、提交后身份回问防护）。含两个非 happy path 小节，各附具体样例片段：①parse 失败修复流程；②语义守卫拦截的对话样例
- **Ch4 SFT 数据过滤**：L3 tagger、NLI/RPCR verifiers、funnel、分层采样。含**决策插叙①**：L2 NLI premise-per-message——为什么 NLI 的 premise 要逐条 message 构造，影响了什么（出处：research-proposal/adr-l2-nli-premise-per-message.md，注意核实）

**代码文件**（写入 `recap-code/`）：

- `03_sft_synthesis.py`
- `04_sft_filtering.py`

## 代码写法约定

- 语法合法的 Python，可读但不可运行；保留真实 import 路径、函数签名、类型标注、控制流骨架；省略处用 `...` 或一行注释带过
- **函数名必须使用项目中的真实函数名**（在 `/Users/sunxichen/Projects/agentic-gov` 源码中核实存在，重点看 `src/agentic_gov/synthesis/`、`verifier/`、`l3_tagger/`、`sampler/`）
- 中文注释，解释函数**内部执行逻辑**和为什么这么做，不是只写输入输出
- 文件顶部 docstring 说明该阶段在全链路中的位置

## 写作风格（重要）

- 中文，技术术语保留英文原词
- **不要黑话、不要营销腔**。像一个资深算法工程师在梳理自己的项目、向其他工程师介绍。类似 All-in-x / Hands-on-x 技术 blog 的平实风格
- 不只写 happy path
- 决策插叙要讲清楚：当时遇到什么问题 → 有哪些选项 → 为什么选了这个 → 结果如何

## 协作约定（多个 writer 并行写同一个 blog 文件）

- 你只写 Ch3-Ch4。把内容写到 `recap-blog.d/ch03-04.md` 分片文件（目录不存在就自己建），**不要直接写 recap-blog.md**，最后由 orchestrator 合并
- 章节标题格式：`## Ch3 SFT 数据合成：...`
- 与其他章节的衔接用一句话过渡即可

## 约束

- 只读 agentic-gov 和 ART，不修改它们的任何文件
- 写入位置仅限 `.scratch/interview-deck/agentic-gov-recap/` 下的 `recap-blog.d/ch03-04.md`、`recap-code/03_sft_synthesis.py`、`recap-code/04_sft_filtering.py`
- 完成后回复：产出文件清单、每章小节结构、你引用的关键源码文件、你认为需要 orchestrator 注意的问题
