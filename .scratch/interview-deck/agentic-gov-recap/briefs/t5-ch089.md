# T5 Brief: Blog Ch8-9 + 07_rl_rollout_reward.py

## 背景

我们在为已完成的研究项目 **agentic-gov**（位于 `/Users/sunxichen/Projects/agentic-gov`）撰写面试复现材料。项目是政务（公积金）task-oriented agent 的 SFT→RL 全链路研究，共 6 个 phase（sandbox 环境/任务工厂 → SFT 数据合成+过滤 → SFT 训练 → user simulator → release gate → ART GRPO RL）。RL 基于 OpenPipe ART 框架（源码 `/Users/sunxichen/Projects/ART`），项目侧 RL 代码在 `phase6/`。

最终交付物是两份同等地位、相互独立的材料，供 20-30 分钟面试自述：

1. **recap blog**：`/Users/sunxichen/Projects/leetcode-practice-app/leetcode-practice-web/.scratch/interview-deck/agentic-gov-recap/recap-blog.md`（单文件长文，多个 writer 分章撰写）
2. **recap code**：同目录 `recap-code/` 下的 8 个伪代码文件

## 先读

- 同目录 `spec-recap-blog.md` — 完整规格（你的章节大纲在里面）
- 同目录 `tickets.md` — 你的 ticket 是 T5，验收标准以它为准
- 同目录 `fact-base.md`（如已存在）— 事实底稿，**只是参考起点，不是唯一来源；你必须自己通读项目相关源码与文档核实细节**

## 你负责的部分

**Blog 章节**（写到分片文件，见协作约定）。这两章是 RL 主菜的前半：

- **Ch8 RL 数据与采样**：learnability pool（可学习性池——什么样的任务值得进 RL）、采样 frontloading、variance-aware mixture sampler。决策插叙⑩：SR5 数据血缘/课程问题的发现与修复（出处 docs/experiment-notes/028、029、030 及相关 ADR，注意核实）
- **Ch9 Rollout 与 Reward**：rollout 架构（sim server、vLLM serving）、adjudicator（裁判打分）、reward 设计从 v2 到 v3 的演进（terminal-gated outcome）。两个决策插叙：⑥format failure 处理 hard-zero vs resample（出处 research-proposal/adr-format-failure-hard-zero-vs-resample.md）；⑦reward v2 质量天花板 → v3 重设计（出处 handoff/handoff-phase6-grpo-reward-v2-quality-ceiling-1-20260624.md 等）。含一个非 happy path 小节：format hard-zero 轨迹、Escalate / FinishWithRefusal 轨迹的具体样例

**代码文件**（写入 `recap-code/`）：

- `07_rl_rollout_reward.py`

## 代码写法约定

- 语法合法的 Python，可读但不可运行；保留真实 import 路径、函数签名、类型标注、控制流骨架；省略处用 `...` 或一行注释带过
- **函数名必须使用项目中的真实函数名**（在 `/Users/sunxichen/Projects/agentic-gov` 源码中核实存在，重点看 `phase6/art/rollout.py`、`rl_task_pool.py`、`learnability_pool*.py`、`scenario_sampler.py`、`sim_server.py` 及 reward 相关模块）
- 中文注释，解释函数**内部执行逻辑**和为什么这么做
- 文件顶部 docstring 说明该阶段在全链路中的位置

## 写作风格（重要）

- 中文，技术术语保留英文原词
- **不要黑话、不要营销腔**。像一个资深算法工程师在梳理自己的项目、向其他工程师介绍。类似 All-in-x / Hands-on-x 技术 blog 的平实风格
- 决策插叙要讲清楚：当时遇到什么问题 → 有哪些选项 → 为什么选了这个 → 结果如何
- RL 概念只在项目真实用到的语境里解释，不做通用 RL 科普

## 协作约定（多个 writer 并行写同一个 blog 文件）

- 你只写 Ch8-Ch9。把内容写到 `recap-blog.d/ch08-09.md` 分片文件（目录不存在就自己建），**不要直接写 recap-blog.md**，最后由 orchestrator 合并
- 章节标题格式：`## Ch8 RL 数据与采样：...`
- Ch10（ART GRPO 训练）由另一个 writer 负责，你在 Ch9 结尾用一句话过渡即可，不要展开训练循环细节

## 约束

- 只读 agentic-gov 和 ART，不修改它们的任何文件
- 写入位置仅限 `.scratch/interview-deck/agentic-gov-recap/` 下的 `recap-blog.d/ch08-09.md`、`recap-code/07_rl_rollout_reward.py`
- 完成后回复：产出文件清单、每章小节结构、你引用的关键源码/文档、你认为需要 orchestrator 注意的问题
