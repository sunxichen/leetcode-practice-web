# agentic-gov 项目深问阶段 · 完整答案（v2，三方一致定稿）

> 版本史：v1 草稿 → 两位独立专家（新 tab、新上下文）审阅，共 5 项阻塞级问题（G1-2 的 R_terminal 二值契约、G7-1/G5-5 的 CISPO 裁剪区间 [0,5]、G7-1 的 advantage 稳定常数 1e-6、G3-4 的根因张冠李戴与"待重测"过时口径）+ 约 20 项非阻塞修订，全部采纳修复 → v2。
> 审阅记录：`reviews/ans-ds-review-v1.md`、`reviews/ans-gpt2-review-v1.md`。

> 配套文件：`agentic-gov-deep-dive-questions.md`（v3 定稿，41 题）。
> 用途：第一次记忆用，写**完整答案**而非要点卡。风格要求：不说黑话，术语第一次出现时用大白话解释，像两个懂技术的同事正常交流；不按面试时长裁剪，讲清楚为止，现场由自己把控节奏。
> 纪律：与问题集相同——"计划/预注册/待执行"和"已执行/当前有效"严格分开；历史设计讲的时候标明是历史。
> 每题末尾标注主要依据文件，方便回查。

---

## G1 Reward 与判定设计

### G1-1 你的 reward 从 v1 改到 v3，每一版在修什么问题？为什么上一版不够？

我们的 reward 改过三版，每一版都是被具体的坏例子逼出来的，不是为了改而改。

v1 的问题是个语义瑕疵：我们把扣分项（比如超轮数、工具调用失败）算进了得分预算里，结果一个完美走完的 episode 最多只能拿到 0.75 到 0.80 左右。这意味着"满分"不代表"全对"，分数的天花板本身含义就不干净，后面所有基于分数的统计都会带着这个歧义。

v2 修的就是这个：把正向的质量项重新归一，让完美 episode 能真的拿到 1.0。修完我们做了个事后验证，拿历史上 728 条 rollout 按 v2 的公式重算，组内分数标准差平均提升了约 30%——也就是说同样一批轨迹，v2 能给出更强的组内对比信号；同时全同分组的数量没变，说明这个改善不是靠掩盖零方差组做出来的。但 v2 留下了一个它没修的问题：终局动作的对错没有单独把关。典型场景是那种不需要写数据库的任务（比如纯查询、或者该拒绝的任务），这种任务做对做错最终状态都一样，所以"状态对"和"动作对"会打平——agent 选错了终局动作（比如该拒绝却强行办结），分数上可能跟选对了没区别。

v3 就是冲这个来的：把完成度拆成"状态对不对"（R_state）和"终局动作对不对"（R_terminal）两个因子，用乘法门控，R_complete = R_state × R_terminal。终局动作错了，状态分再高也清零。两个因子各自都还能单独看，所以可诊断性没有丢。

一个口径上的提醒：v3 现在是实现好的、现役的版本（代码在 reward/aggregate.py，配置有 sha256 绑定校验）。但历史上有个 T2 probe 阶段实际跑的时候接错了判定器（接成了 NoHitChecker），那段时期的读数不能拿来代表 v3 的行为——这个事故本身是 G4-6 的故事。

依据：`adr-phase6-grpo-reward-v2-quality-ceiling-1.md`、`adr-phase6-grpo-reward-v3-terminal-gated-outcome.md`

### G1-2 为什么终局正确性用乘法门控（R_state × R_terminal）而不是给 FWR 加一个对称的奖励项？R_escalate 为什么被移出训练总和？

这其实是两个不同的问题，我分开说。

为什么是乘法而不是加法。关键在于"状态对"和"动作对"在我们这里是一个"且"的关系：两件事都对了，这个 episode 才算成。如果用加法，状态全对、动作错了，agent 还是能拿到大部分分——这就把 v2 想修的那个"选错终局动作不吃亏"的问题又放回来了。也有人提过折中方案：给那个最容易被委屈的动作（带拒绝的办结，我们叫 FWR）单独加一个对称的奖励项，把这个洞补上。我们没接受，因为这本质上是给"终局正确性"这一件事拆出了一个非对称的补丁：别的动作靠乘法管，唯独它靠加分管，规则的统一性就没了，以后每加一类终局动作都得再想一遍要不要补丁。乘法门控是一条统一的规则，对所有终局动作一视同仁。

R_terminal 本身的设计也配合这个思路：它是一个二值门，只取 0 或 1——agent 实际执行的终局动作和任务期望的动作精确一致得 1，否则得 0。设计文档里说的"对称精确比较"，指的是三种合法终局动作（办结、带拒绝办结、转人工）之间一视同仁、没有任何动作有特权，不是说分数有三档。而且它是失败安全的：终局动作的结构化记录缺失、解析异常或不可靠时一律按 0 处理，绝不反过来从最终数据库状态去猜 agent 干了什么。

R_escalate 移出训练总和是 v3 顺手做的另一件事。原来给它留了 0.10 的权重，初衷是鼓励该升级人工时别犹豫。但后来我们意识到，终局动作的正确性已经被 R_terminal 完整管起来了，再额外给某一个动作发奖金，等于在同一个信号上重复计费，还会扭曲各动作之间的相对收益。所以 v3 把它从训练用的总分里拿掉了，只在报表里保留观测。

依据：`adr-phase6-grpo-reward-v3-terminal-gated-outcome.md` 决策三、决策五、§10

### G1-3 有人说"把 reward 线性归一化到 [0,1] 更自然"，为什么这对 GRPO 基本是空操作？它和你们实际做过的 v2 ceiling 修正有什么区别？

我们内部确实认真讨论过"把整个原始分线性缩放到 0 到 1"，最后否决了，原因很简单：GRPO 用的不是绝对分，而是组内相对分。具体说，同一个任务采 K 条 rollout，每条的"优势分"是它的原始分减去组平均、再除以组内标准差。你对所有分做一个线性变换（乘个系数再加个常数），减均值的时候常数消掉了，除标准差的时候系数也消掉了——每条轨迹的优势分一个字都不变。全同分的组原来没有梯度，缩放后还是没有梯度。所以纯线性缩放对训练是严格的空操作，只是让报表上的数字好看一点。

v2 做的 ceiling 修正跟这个有本质区别：它不是对总分做线性变换，而是重新分配了内部质量项的权重，让"完美"真的等于 1。这改变的是组内分数的相对结构——做对和做错之间的差距变了，部分得分的分布也变了，所以它不是空操作。事后验证也证实了：同样 728 条历史 rollout，v2 口径下组内标准差平均大了约 30%。

一句话总结：线性缩放改的是"分数的尺子"，GRPO 不看尺子；v2 改的是"分数本身的相对距离"，这个 GRPO 看得见。

依据：`adr-phase6-grpo-sampling-frontloading` §4.3、`adr-phase6-grpo-reward-v2` §3

### G1-4 为什么 reward 在 episode 结束统一结算？这种 terminal-only 设计在多轮信用分配上的优势和局限分别是什么？

选终局统一结算，核心理由是：在我们这个环境里，只有"结局"是可信赖的。episode 结束时，数据库最终状态可以程序化比对，终局动作有结构化记录，这些信号很难被 agent 刷出来。而过程中的信号——比如"这一步走没走在标准路线上"——要么不可信，要么容易教出奇怪的行为。

我们认真评估过三种过程奖励，全部否了。一是按步骤比对标准路径给分：这会惩罚那些"绕了路但最后做对"的轨迹，而 GRPO 恰恰靠多样的尝试来学习，把路径锁死等于把探索杀掉。二是给"从错误里恢复"单独加分：它和完成度高度重叠，恢复成功的 episode 本来完成度就高，重复计费。三是给"没有冗余调用"加分：这跟我们的轮数惩罚覆盖的是同一件事。思考链我们也不打分——给内心独白打分是在教模型"写得像在想清楚"，而不是真的想清楚，这是最经典的 reward hacking 入口。

但局限性必须诚实说：终局结算意味着整条轨迹只拿到一个总分，这个分数形成的优势分会被平摊到所有 assistant token 上。一条 8 轮对话的轨迹，可能是第 2 轮问对了关键问题才成功的，也可能是第 6 轮一个错误操作把局面搞砸的——reward 本身定位不了是哪一轮的功劳或责任，这就是多轮信用分配问题，我们没有解决它。我们做的是缓解：把总分拆成完成度、告知、效率几个子项分别看，再加上组内 8 条轨迹互相对比，让信号的方向性和可诊断性更好、方差更小。如果面试官追问真正的解法，业界方向是过程奖励模型或者按轮次归因的 advantage 估计，那是牺牲可验证性换粒度，我们评估后认为现阶段不值得。

依据：`最终研究方案.md` §6.1/6.5/6.8、`golden_chain终稿` §三

### G1-5 disclosure（"说到位"）为什么纯 NLI 做不下去？hybrid 判定链怎么分层？NLI 的 premise 为什么按每条 assistant 消息取 max？

"说到位"要判断的是：该告诉群众的信息（租金上限、所需材料、办理时限这些），agent 是不是真的用自然语言说出来了。这是一个语义判断，规则匹配做不了，所以一开始我们很自然地上了 NLI——用一个自然语言推理模型判断"对话内容是否蕴含了该告知的事实"。

纯 NLI 死在一个结构性问题上：判对判错的阈值在不同任务之间差了三个数量级。也就是说，不存在任何一个全局阈值能把所有任务同时判对——有一批任务（编号 P-07、N1-03 这些）在任何阈值下都做不出来。还有一个更隐蔽的问题：我们用的 mDeBERTa 模型最多吃 512 个 token，一整段对话塞进去必然截断，而截掉的恰恰是对话末尾——告知往往就发生在最后几轮。实测数字很直观：把整段对话当前提去判，得分 0.0032；换成逐条消息去判，同样内容得分 0.9971。所以 premise 按每条 assistant 消息分别取最大值，不是工程优化，是救命——它保证每一轮 agent 说的话都被完整喂给模型，"任意一轮说到了"就算说到。

最后的方案是三层链。第一层本地 NLI 先判，大部分样本在这里就有结论；第二层是确定性的关键词兜底，处理 NLI 明显不靠谱的形态；第三层才交给大模型裁判（adjudicator），只处理前两层拿不准的。链的方向是不对称的，这是刻意的：对于"该说"的槽位，本地判"说到了"要送给裁判复核——因为有些安慰性话术听起来像告知但其实是空话，容易误判过线；对于"不该说"的槽位（比如内部工号不能泄露），本地判"没说"就直接安全放行，只有判"说了"才送裁判终裁——因为否定语境（"我不能告诉你工号"）容易触发假阳性。总规则是：负面槽位一票否决，正面槽位按命中比例计分。

还有个插曲值得讲：我们第三版校准集（rc-3）曾经退化成"一句话复制一百遍"，在这种数据上 F1 刷到了满分——是对抗审计把这个假象戳穿的。这也解释了为什么后来我们把判定器的运行时身份审计看得这么重。

依据：`adr-phase5-reward-divergence.md` 决策一、`adr-l2-nli-premise-per-message.md`、Note 008/009

### G1-6 同样一个失败动作，hard-zero、负分、拒采重采三种处理在 reward 几何和责任归属上差别在哪？你们为什么最终是"hard 归零 + efficiency 继续"？

先交代背景：我们把失败分成三类。"做了不该做的事"——调了不存在的工具、调了没权限的工具——这是红线，归 hard 类。hard 桶里其实还有另一类：输出信封解析失败、格式非法、动作契约不满足——这些不是 sandbox 拦的，是输出协议层的失败，同样按 hard 归零，但监控里用单独的失败类别标签跟安全违规分开统计，两种失败的含义完全不同。"方式不对"——前置条件没满足、必填参数没填、格式非法——这归 efficiency 类，错误信息会原样回给 agent，episode 继续。还有一类其实不是错误：业务拒绝，比如资格审查不通过，一共 14 种，这是 sandbox 在正确工作，完全不罚。

对 hard 类，候选处理有三种。第一种是负分：给负奖励但不终止。我们没选，因为负分的幅度是一个全新的设计自由度——负多少算合适？这个数定完跟正分项的比例关系就变了，而且不同版本之间没法比。注意我不说"负分会扭曲组内基线"这种话——那不是普遍成立的数学结论，只是某些实现下的风险。第二种是拒采重采：这条轨迹不算数，重新采一条。我们也没选，因为这等于告诉模型"格式错误不用你负责"，它永远学不会去避免——责任被环境替他扛了。第三种是归零终止，我们选的这个：语义上，踩了红线的 episode 就是没有任何可接受的贡献，给 0 分是一个绝对门控，含义干净；责任上，错误记在 agent 自己头上，它必须学会不犯。

efficiency 类为什么不终止、只让小错自然发生？这是 GRPO 的学习方式决定的：它靠试错学习，恢复能力本身就是要学的技能。如果第一次参数填错就终止 episode，模型永远见不到"犯错之后怎么办"的剧本。我们的设计是被拦截后改正重试，最终完成任务照样拿满完成度分——这样"恢复"在 reward 上是完全值得做的行为。

实测上这个选择也站得住：格式类失败率约 2%，低于我们 5% 的警戒线；业界 DeepSeek-R1 的做法也是格式失败直接零分，有先例。

依据：`adr-sandbox-error-hard-vs-efficiency.md`、`adr-format-failure-hard-zero-vs-resample.md`

### G1-8 0.65/0.35/0.10 这些权重是怎么定的？做过敏感性分析吗？

诚实回答：这些权重不是调出来的，是继承加定性判断来的。

主线比例（完成度对告知大约 2:1）从 v1 就定下来了，依据是业务直觉：这个事首先是"事办对没有"，其次是"话说到没有"，所以完成度占大头。v2 里我们把给"升级人工"动作的额外奖励从 0.05 提到 0.10，文档里记录的理由是：同一轮采样器调整把更多预算投向了两类办结任务，提高升级人工的权重是为了对冲终局动作边界被挤压的风险——这是一个定向的定性调整，不是扫参扫出来的。到 v3，这个额外项整体移出了训练总分（理由见前面 G1-2），所以现在训练在用的就是完成度和告知两项，加效率罚。

敏感性分析——系统地扰动权重看结果变化——我们没做，这一点我不打算包装。原因有两个。一是算力账：我们总共两张 4090，一轮敏感性分析意味着好几组完整训练，当时判断把 GPU 花在终局效果验证上价值更高。二是归因账：v2 上线那一轮恰好和采样器改动同一轮启用，两个变量一起变了，我们书面承认了那次归因不干净，后来用 v1 的影日志（只记录不参与训练）补了一部分可解释性。在这种历史背景下，就算当时做了敏感性分析，结论也得打个问号。

这个题我自己的总结是：知道哪里不严谨、并且能说出为什么不严谨，比假装严谨重要。权重是我们的弱项之一，我清楚它的边界。

依据：`adr-phase6-grpo-reward-v2-quality-ceiling-1.md` §3.2、决策二/五

---

## G2 环境、数据与 Simulator

### G2-1 contrast pair（只差一个边界因子、正确动作相反的 A/B 对）在 GRPO 里起什么作用？为什么 A、B 绝不能混进同一个组？

先说什么叫 contrast pair：我们造了一批成对的任务，A 和 B 在用户开场白、人物设定、可透露规则上完全一致，只在后台世界里差一个关键事实——比如 A 的申请金额在限额之内、B 超出限额一点点。结果就是：同一个开场，A 的正确结局是办结，B 的正确结局是拒绝或转人工。它考验的是模型能不能靠追问和查证把这两条路区分开。

在 GRPO 里，这个设计的价值在两头：数据层，它保证决策边界的两侧都有覆盖，模型不会只见过"该办"的世界；评测层，我们有专门的 contrast-set 桶来度量"边界区分能力"这个指标。

但它有一个绝对禁区：同一个 pair 的 A 和 B 不能进同一个训练组。原因是 GRPO 的基准线机制——每条轨迹的优势分是跟组内平均比出来的，这个机制成立的前提是组内 8 条轨迹面对的是同一个问题，组平均才是公平的基准。A 和 B 混在一组，基准线就变成"两个不同问题的混合平均"。举个具体数字：假设 A 简单，大家平均拿 0.8；B 难，平均拿 0.3；混组后组均大约 0.525。于是一条做错了的 A（拿 0.75）在组里显得高于平均，被强化；一条做对了的 B（拿 0.30）显得低于平均，被抑制。净效果是系统在教模型"别费劲区分了，往办结那边偏"——这正是 contrast pair 本来想治的病。所以我们的采样规则是：pair 必须成对出现在不同组，子采样也必须成对走，拆开剩下的半边大多落在办结侧，会加剧偏科。

依据：`adr-phase6-contrast-pair-grpo-grouping.md` §4

### G2-2 simulator 为什么看不到工具的返回结果？给它看会怎么样？

我们的 simulator 扮演的是群众，它的输入只有一件事：对话历史里的自然语言——用户说了什么、agent 说了什么。它看不到 agent 调了什么工具、更看不到工具返回的 JSON。这不是技术限制，是刻意画的信息边界：真实世界里，坐在柜台对面的群众看不见工作人员在哪个系统里查了什么，他只听得见工作人员跟他说的话。

如果把这个边界拆了，让 simulator 看到工具结果，会发生三件事，一件比一件糟。第一，simulator 会学到一条捷径：不用等 agent 把信息说成人话，直接从工具返回的 JSON 里抄——它的追问和反应会基于 agent 没说出口的信息，对话就不再真实。第二，更致命的是对 agent 的反向激励：agent 会发现"只要调了工具，群众就像知道了一样"，于是它不再练习把结果说清楚——而我们整个"说到位"的奖励设计，前提恰恰是信息只能通过自然语言传递。环境保真度一崩，训练信号全部失真。第三，我们的泄漏类指标（RPCR）会变成虚高，因为 simulator"知道"了不该知道的信息，真实泄漏被掩盖。

实现上这个边界就一行逻辑：喂给 simulator 的对话历史只保留 user 和 assistant 两个角色的自然语言轮，工具调用和工具返回全部剥掉，连续同角色的轮次合并成一条。

依据：`adr-simulator-information-boundary.md`、`frozen_simulator_backend.py::_normalize_history`

### G2-3 simulator 的 reveal 偏急（被问即答而非拖一轮，残余泄漏 10-14%）为什么不阻断训练、也不把泄漏做成负 reward？什么情况会改判？

先说现象：我们设计里，群众对敏感信息应该"先含糊一下、被追问才说"，模拟一种自然的戒备心。实际实现的 simulator 偏急——agent 一问它就答了。量级口径要记准：带这条"拖一轮再说"规则的任务约占任务池的 10% 到 14%；实测的泄漏率比这低——购房类任务约 10%，非贷款类约 3%。

这个问题要不要阻断训练，我们专门开了一份决策记录来回答，判据就一句话：它会不会污染 GRPO 的训练信号。分析下来不会，理由有三层。第一，泄漏基本不进 reward：核心分量（最终状态、终局动作、告知情况）都不依赖"群众第几轮才说实话"。要诚实的是存在一条间接通道——轮数惩罚：群众答得太痛快，对话可能变短，理论上会影响轮数分布；但实测这个影响在同组 8 条轨迹间是低方差的共同偏移，不改变组内排序。而且 simulator 在我们设计里是冻结的环境的一部分，罚一个 agent 管不了的环境行为，只会往梯度里注噪声。第二，这个泄漏是被动的——是 agent 问了它才答，不是它主动抢答，所以它不会教 agent"不用追问也能拿到信息"这种坏习惯。第三，对组内对比的影响有限：GRPO 比较的是同组 8 条轨迹的相对表现，如果泄漏偏差在这 8 条之间大致一致，它就近似一个共同的平移，不改变组内排序。当然这个论证是有前提的：偏差得近似一致、不能反过来改变 agent 的后续行为、不能跟 reward 耦合。目前审计显示它是低频且一致的偏移，不是高方差污染源，所以前提成立。

同时我们写了四条明确的改判条件，触发任何一条就推翻结论：泄漏从被动变成主动抢答；泄漏占比超过 30%；reward 开始依赖信息透露的时机；泄漏内容进入 reward 计算。这四条写在决策记录里，不是口头约定。

依据：`adr-simulator-delayed-reveal-not-blocking-phase6.md`、`phase5-reward-pipeline.md` §1

### G2-4 数据全合成，凭什么敢用？一条任务从生成到进训练池要过哪些关？

这个问题分两层回答：单条数据对不对，和整个数据集可不可信。

单条层面，一条合成任务要过六道自动关加一道人工关。L0 格式校验：字段、类型、枚举值合法。L1 sandbox 重放：把这条任务的"标准答案轨迹"（golden chain）真的在仿真环境里跑一遍，跑不通就说明任务定义和环境不自洽。L2 NLI 校验：检查任务文本和标准答案之间的语义一致性。L3 实体校验：任务里提到的人名、金额、编号要前后一致。L4 泄漏检查：开场白和可透露规则里不能提前漏出该隐藏的关键事实。L5 judge 复核：用大模型裁判再过一遍疑难项。六道全过之后还有 L6：按分层抽样的人工抽检，人来看机器有没有系统性看走眼。

数据集层面，防的是另一种病：模型靠"背题"拿高分。我们做了两件事。一是隔离：同一族任务（共享骨架和词表的一窝任务）按 family、skeleton、词汇包三个维度做训练/验证/测试的切分隔离，保证测试集里没有训练集的任何近亲。二是去重门：同一个格子、同一个难度档内，相似度超过 0.90 的任务会被拒掉。另外 P5 的 holdout 有个刻意的特殊设计：一千条任务，每条恰好来自一个不同的族——也就是族数和任务数相等。这样做的效果是把族内相关性压到约等于 1（统计上叫设计效应 DEFF≈1），让"独立同分布"的假设在 holdout 上近似成立，置信区间更可信。注意这是 P5 holdout 的特有设计，不是所有历史数据都长这样。

最后还有一道总闸：任务有效性契约（G4-7 展开讲），任何任务进梯度面或评测面之前要过五个硬问题，过不去直接出局——历史上 247 条"坏题"就是这么退役的（162 条冒充幽灵题加 85 条冻结状态不可观测题）。

诚实的边界：合成数据和真实数据之间的差距（synthetic-to-real gap）没法被任何流水线消除，只能持续监测。我们敢用的理由是"每一类已知的造假路径都有对应的关卡"，不是"合成等于真实"。

依据：`phase2-verifier-pipeline.md`、`funnel.py`、P5 方案 §3-4

### G2-5 contrast pair 是怎么构造出来的？怎么保证 A/B 真的"只差一个关键事实"？

构造从选"边界因子"开始。每个边界因子是一个能把正确动作翻转的最小语义单位——比如"申请金额对比取款限额"这个因子：限额之内该办结，超出限额该拒绝。A 侧取限额内一个值，B 侧取限额外一个值，正确动作就反过来了。注意这只是数值边界的例子——不是所有 pair 都固定"A 办结、B 拒绝"，哪一侧对应什么动作，由每类边界因子自己的契约决定。边界因子分两类：数值型 7 种（大多是各种金额对限额的关系，可以画出一条"边界鲁棒性曲线"），类别型 8 种（比如合同备案状态、资格状态这类离散翻转）。

"只差一个事实"是用一份冻结字段清单机器强校验的。A 和 B 两侧必须逐字段相同的包括：人物设定的全部 9 个维度、开场白一个字节都不能差、信息透露规则逐键相等、模糊度画像逐键相等、可透露槽位清单排序后相等、任务类型相同、政策版本相同。允许不同的只有边界因子本身，以及它联动派生的字段——比如金额因子会同时改后台数据库里的申请金额和对应的政策参数，这些联动是登记在案的合法翻转。

有两个设计细节值得说。第一，为什么开场白可以一字不变：因为边界因子定义在后台状态层，不在用户嘴里。A 和 B 的用户说同一句"我买了个房，想取公积金"，差异只在 agent 调用查证工具后拿到不同返回值。这里有一条硬规则：被边界因子改动的字段，透露规则只能是"被问才说""被直接追问才说"或"从不说"，绝不能"开场主动说"或"拖一轮再说"——否则要么开场白和后台事实自相矛盾，要么差异被延迟机制污染，最小对照就不成立了。第二，B 侧的标准答案允许"先错后恢复"的两步剧本：先按错误金额提交被环境拒绝，再修正重试成功——这让 B 侧不只是一条"该拒绝"的题，还是一条"该会从错误里恢复"的题。

依据：`phase2-contrast-set-spec.md` §2、`entity_preserving/`、`adr-phase6-contrast-pair-grpo-grouping.md` §4.2

### G2-6 Policy Card、API Spec、sandbox precondition 和 reward 分别管什么？为什么不能把所有规则塞进 prompt、或全写死成 workflow？

这四层各管一段，可以按"谁在什么阶段、以什么力度约束 agent"来分。

Policy Card 是政策知识卡，agent 能直接读到，管的是"业务上该怎么决策"——什么条件该办结、什么情况该拒绝、什么情况该升级人工。它是给模型学的知识，答案在里面，但模型得自己判断什么时候用哪条。

required_slots 管对话侧：办这件事按规定应该主动收集哪些信息，它决定 agent 该追问什么。

API Spec 的必填参数管工具执行侧：调这个工具，契约上必须带哪些参数，少一个调用就是非法的。

sandbox 前置条件是最硬的一层：结构性约束，不满足就直接拦截。比如校验动作和提交动作必须是同一个业务对象——验了 A 的额度却提交 B 的申请，写库前就拦下来。这一层的语义是"有些事结构性上不许发生"，不靠模型自觉。

reward 管最后一层：对所有"合法"的行为做质量排序。前三层决定什么能做，reward 决定什么算做得好。

为什么不能合并？塞 prompt 的问题是没有强制力——上下文一挤、注意力一漂，模型就"忘了"，而且把所有规则塞进 prompt 会吃掉宝贵的上下文预算。全写死成 workflow 是另一个极端：那模型就退化成流程执行器，学不到任何非确定的决策——什么时候追问、追问什么、终局选哪个动作，这些恰恰是我们要训练的能力。设计原则可以概括成一句：强规则结构性保证（sandbox 拦），弱信号可学习（reward 排），知识显式可读（Policy Card），三者各司其职。另外政策标识和版本在数据合成期和运行期是硬绑定的，保证训练时学到的规则和执行时强制的规则是同一版。

依据：`最终研究方案.md` §5.5、`Policy Card与Agent Skills讨论总结.md`

---

## G3 训练、采样与稳定性

### G3-1 你说用 GRPO，但实际优化的 loss 不是教科书 GRPO——准确口径是什么？CISPO 和 PPO clip 的梯度行为差在哪？

先把口径说准确，因为这是面试最容易被抓的点。我们说的"GRPO"，实际是两件事的组合：优势分（advantage）的计算用 GRPO 的方式——同一个任务采 8 条 rollout，每条的优势分是它跟组内平均的差距除以组内标准差，全程没有价值网络（critic）；但逐 token 的损失函数不是教科书 GRPO 附带的那种 PPO 式裁剪，而是 ART 框架默认的 CISPO。

CISPO 和 PPO 的差别在一行代码上：新旧概率比率（当前策略产出这个 token 的概率除以采样时旧策略的概率）先 detach——也就是切断梯度、只当一个标量权重用——然后裁剪到上限，乘上优势分，再乘当前策略的 log 概率。整个式子里梯度只从"当前 log 概率"那一项流。PPO 不 detach：比率本身带梯度，目标函数取"未裁剪"和"裁剪"两个分支的较小者。

这个差别的后果要按优势分的方向说。PPO 里，当一个 token 的更新方向要把策略推出信任区时（比如优势分为正、想提高它的概率，但比率已经超过上界），就进入被裁剪的分支，这个 token 的梯度就没了——它被禁言了。CISPO 里被裁剪的 token 只是权重被压到上限，梯度还在，行为像一个带着折扣权重的 REINFORCE（最经典的策略梯度）。对我们这种多轮、长序列的任务，关键 token 很稀疏——整条轨迹里真正决定成败的可能就是调哪个工具、选哪个终局动作那几个 token，它们的概率变化往往最大、最容易触发裁剪。PPO 会把这批最有信息量的 token 禁言，CISPO 不会。所以对我们，CISPO 是净收益。

补一个诚实的注脚：我们主线是严格同策略（strict on-policy，采完立刻训、训完再采），新旧策略本来就几乎一样，实测比率被裁的比例极小（10 的负 6 次方量级）。所以平时 CISPO 的行为约等于不裁剪的普通策略梯度，它的价值体现在一旦有偏移时不会静默丢梯度。

依据：Note 024、`ART/src/art/loss.py:188`

### G3-2 组内全对或全错的 rollout 组怎么办？为什么"越难越该多采"被明确禁止？采样预算按什么定？

全对和全错在我们眼里是同一种东西：组内 8 条分数一样，优势分全是零，梯度方向不存在。这类组叫零方差组，处理方法是动态过滤——直接丢弃，不参与梯度更新。省的是无效的优化器步和统计污染。有一个口径要小心：过滤发生在 reward 结算之后，所以它省不了已经花掉的判定成本，只省训练侧的浪费。

"越难越该多采"听起来直觉，实际是被我们明文禁止的。数学原因很简单：组内信号的强度正比于方差，而二值成败的方差在成功率一半对一半时最大。成功率接近 0 的组是全错死区，采多少都是零方差；成功率接近 1 的是全对饱和区，同理。按"难度倒数"加权采样，等于把预算往死区里扎。

所以我们的采样预算按实测的组内方差分布来定，具体操作是给每个任务桶打"可学习性"分档：先用冻结的 8 条探针（K=8 probe）测每个任务的成功率，落在 2/8 到 6/8 之间的是核心带——这里组内必有对比信号；0/8 到 1/8 的先标"待复测"，不轻易投入；7/8 到 8/8 的算饱和，不指望它产梯度——全对必然零方差、被过滤掉是正常且预期的——但会留少量做金丝雀（canary）：真正要盯的是它的成功分布，哪天金丝雀桶从"稳定全对"变成开始出错，说明模型在退化，那才是报警信号。这就是"预算按方差走，不按难度走"。

依据：`adr-phase6-grpo-step2-variance-aware-mixture-sampler.md` §5.5、`learnability_pool_v2.py`、`train_grpo.py::filter_zero_variance_groups`

### G3-3 你们最终的长程 GRPO 训练 schedule 是怎么设计的？怎么防止终局动作偏科？

先把口径钉死：这套 schedule 是 P5/T4 阶段冻结的设计。截至 2026-08-13，第一波 E1（前 100 步）已经执行完成、出了 0100 检查点，但 step-100 的冻结开发集对比评测还没做——所以我说的是"设计怎么定的、第一波跑完了但效果未评"，不能说成"全程跑完了"，也不能再说"没跑过"。E1 执行中还出了两个插曲：启动早期遇到过一次段错误，从检查点恢复；第 60 步被质量熔断误杀过一次（详见 G3-6）。

问题的背景是任务矩阵不均衡：4 类业务 × 3 种终局动作共 12 个格子，其中"该办结"的任务天然多，"该拒绝、该转人工"的任务少。如果无脑随机采，模型会在办结上越练越强、在稀有动作上退化——这就是偏科。

设计是这样的。每步训练采 8 个组，每个组 8 条 rollout。8 个组分三个槽位类别：4 个稀有动作核心槽，每类业务各来一个，终局动作是拒绝或转人工，优先从可学习带（2/8 到 6/8）里挑；2 个办结锚点槽，轮流覆盖不同业务，作用是防办结能力回退，注意这里即使整组全对也不强行制造梯度——锚点的意义是监控和保护，不是硬凹信号；剩下 2 个广度槽，轮流照顾曝光不足的格子。

步与步之间还有浓度上限，按滚动 50 步一个窗口算：12 个格子每个至少占 5%、至多 15%；三种终局动作各自锁在一个比例区间（办结 25-35%，其余两种各 30-40%）；同一任务在一个窗口里最多出现 4 次，同一任务族最多 8 次。这套上限防的是"稀有池被刷爆"——比如贷款还款查询类里"转人工"这个格子，全池可学习的任务只有两条，不加限就会无限重复那两条。

组被过滤掉太多时有确定性的补位规则（借鉴 DAPO 的思路但我们做成了完全确定性版本）：目标是每步至少 6 个有效组，不够就从预冻结的按格排队里补，优先补同终局、同格的可学习任务；每步最多补 2 个，全程 500 步最多补 200 个；某个格子连续三次颗粒无收或 20 次提交有效产出率不到 25%，就标记为"无产出格"，停止盲补，交回人工审。

超参全部沿用已验证的组合：学习率 1e-5 余弦退火到 3e-6，KL 系数 0.08，K=8，随机种子根 20260609，每 50 步存一次检查点，每 100 步做一次开发集评估。原则是不在无依据的情况下发明新超参——仓库里唯一有成功背书的组合就是这个。

历史上还有过一个 Step-2 的固定配比采样器（办结类按 0.74 比 0.26 配、每 4 步插一次金丝雀），那是诊断期的历史设计，已经被上面这套取代，讲它时必须标明"历史"。

依据：`expert-consult-p5-t4-planning-20260812.md`、`expert-consult-p5-t4-r2-scheme-20260812.md`

### G3-4 任务的训练顺序和难度递进怎么安排？有没有任务被判定"梯度够不到"？

我们的方案是阶梯式课程（curriculum）：目标桶太难、8 条全败没有任何梯度时，不硬上，而是搭三级桥——L1 最简单、L2、L3、最后到目标任务本身。每一级都用冻结的 8 条探针测成功率，只有落在 2/8 到 6/8 可学习带的级别才进梯度训练；某一级练到饱和（7/8 以上）就退出梯度、降级为监控对象，人往上一级走。

这套设计有几个刻意收窄的决策。一是只保留同质 K=8：早期方案里最难的桥要用 K=16（每组 16 条），后来砍掉了，因为 K=8 和 K=16 混训时，大组天然对损失贡献更多，而我们没有一份被接受的加权合同来证明这个贡献符合目标配比——在验证"由易到难能不能迁移"之前，为 K=16 付出的实现和验证成本不值。二是 K=32 只留一个用途：给最简单的 L1 做"0/8 是不是真死区"的终裁测试——32 条还全败，才允许另案提议用监督微调做助推；它只产证据，绝不更新模型。三是换成员只能在两个预先冻结的边界（练到第 15 步、第 30 步的 C15/C30），段内不换包，保证归因干净。

确实有任务被正式判定过"练不动"，而且值得讲清楚的是：这里有两个不同的格子、两种不同的原因，别混。第一个是购房提取的"带拒绝办结"，被标记为"不具备单调阶梯资格"：检查发现它的 L1 和 L2 两级桥用的是同一套生成配方——同一个概念、同一个边界、同一个人物设定，两级之间的差异只有措辞和无关紧要的歧义，根本没有冻结的难度干预。也就是说"L1 到 L2 逐级变难、逐级接近目标"这个阶梯结构在数据上就不成立。这是课程和路由层面的判定，改 reward 版本解不了。第二个是购房提取的"转人工"：它的 L1 任务曾经全部落在一个监督微调从未供给过样本的手工构造触发类里，基础能力是零——这属于"学不了"，不是"阶梯不成立"。这个已经在 2026 年 8 月 11 日修复（L1 的触发类从手工类整体换成有监督数据供给的冻结类），第二天用 10 个任务乘 8 条共 80 条 episode 重测确认修复生效；最终的可学习性总验收八项全过，这个格子从零条可学习恢复到九条。注意口径：这一条是已执行、已确认的事实，讲的时候不要说成"还在等确认"。

另外一个调权纪律：课程各阶段的任务权重，现阶段只做离线人工调整。自动在线调权（训练中按表现自动改配比）明确留到以后，理由是小样本加短窗口下，在线信号噪声太大，自动调节会把噪声当趋势，而且出了 oscillation（来回震荡）很难归因。

依据：Note 027/028、`adr-phase6-grpo-reward-v3` §8

### G3-5 KL penalty 在你们项目里防的具体是什么？为什么不在 GRPO 池里多混 SFT 数据来防遗忘？

KL 惩罚在我们这里防一个非常具体的病，我们内部叫它"政务复读机"。机制链条是：告知奖励（该说的要说到）加上轮数惩罚（别拖太久）双重压力下，模型会发现一个"最优解"——开场就用一段压迫性的超长话术，把所有该告知的信息一次性倾倒完。分是高了，但说话完全不像人了。KL 惩罚的作用就是把 RL 训练后的分布拴在监督微调后的参考模型附近：你可以优化奖励，但不许偏离"人话"太远。这防的不是任务能力下降，是语气和格式的漂移。

为什么不往 GRPO 采样池里多混监督微调的旧数据来防遗忘？因为这两件事在数学上不等价。往 rollout 池里混更多旧任务样本，这些样本对当前模型来说大多已经饱和——8 条全对，零方差，过滤掉，不产生任何梯度。"多采"不等于" rehearsed（复习）"。要真靠数据保留能力，需要的是显式的辅助目标——比如在训练损失里加一项旧数据上的监督微调损失，或者回放（replay）机制，让模型在旧任务上持续有梯度。我们当前的选择是 KL 锚定：系数历史上从 0.04 调到过 0.08，T4 方案冻结在 0.08。它的好处是零额外数据成本、实现一行，坏处是只能保"分布形状"不能保"具体技能"。所以我们的预案是分层的：KL 锚在线常开，通用指令遵循能力用独立的小评测集持续监控，万一监控报警，再考虑自蒸馏（拿 GRPO 训练前的自己当教师，把能力蒸回来）作为兜底选项，默认不启用。

依据：`方案潜在风险与负面涌现预测.md`、Note 025

### G3-6 长程训练的熔断/停训契约是什么？哪些信号有权直接停训、哪些只能告警？

这套契约是 T4 方案里冻结的，核心是分级非常不对称：有权直接停训的信号极少，大多数问题只能告警或暂停。

能直接自动停训的只有一类：确定性污染。代表性的有——训练里出现 NaN 或 Inf（数值发散，继续训是烧电）；判定器的运行时身份审计确定性失败（说明判定链路被换了，之后所有 reward 都不可信）；数据切分污染（训练集混进了评测集的任务，之后所有评估作废）；判定器静默降级进了梯度路径（裁判超时后本该报错却默默用了兜底值，这种分教出来的东西是垃圾）。完整的注册表里还有身份校验失败、训练收据与恢复状态不匹配、冻结的 schedule 被破坏、simulator 开场泄漏、授权额度超限等，性质都一样：一旦发生，之后产生的所有证据都无效，早停一秒省一秒。

往下一级是"硬暂停加人工门"：新旧策略概率比率的最大值超过 4 就停住等人工裁决（历史实测最大才 1.7 左右）。注意这是"停下来让人看"，不是科学判死——比率尖峰可能是单条异常轨迹造成的。

再往下是告警加确认探针：训练批次里格式错误或硬违规率超过 5%，不直接杀训练，而是触发一个固定的确认流程——用 60 个任务做一轮单侧 99% 置信度的精确二项检验，60 个里至少 8 个复现才算确认问题，才升级到人工。开发集指标（比如验证成功率下降）的权力最小：它只控制检查点有没有资格被晋级为候选，不停训。

为什么这么不对称？因为错杀的代价远高于迟报。一轮长程训练要跑好几周，而小样本告警噪声很大——真失败率只有 2% 的时候，50 个样本里也有约 8% 的概率观察到 3 个以上失败。如果告警线直连停训，训练会被误杀无数次。所以设计哲学是：只有"证据被污染"才配自动停训，"可能有问题"一律先确认再动手。

这套契约在 E1 第一波训练里就实战了一回：第 60 步熔断器把"64 条里 5 条格式异常"判成了硬违规超阈、直接停训——后来查是解析器把一类失败双计了（5 条里 4 条来自同一个组），两位独立专家都判为"很可能是误报"；而且按冻结的契约，格式类超阈本来只该告警加确认探针，不该直接停。最后的处置是不改代码、只改环境变量从第 60 步检查点续训到 100，并把"旧的停训记录必须书面作废"写进交接——不然审计上会留下一次假的"科学停训"。这个故事恰好说明：分级契约的意义不是写在纸上的，是被真实的误杀事件检验过的。

依据：`expert-consult-p5-t4-nonr2-hardstops-20260812.md`

---

## G4 难题复盘

### G4-1 rollout 吞吐的瓶颈是怎么一层层定位的？"busy-time 98% 在 agent"和"GPU 63% 空闲"矛盾吗？

这是我们花时间最长的一个工程问题。rollout 一轮要 20 分钟，目标是压到几分钟。定位分三层，一层一层剥。

第一层查 GPU 拓扑：训练用的 GPU 和 simulator 用的 GPU 会不会互相抢？结论是基本不抢——训练阶段和 rollout 阶段在时间上是互斥的（这叫相位互斥），所以同一张卡分时复用没问题。这一层排除了"硬件不够"这个最容易被冤枉的假设。

第二层查客户端并发：发现 agent 侧的最大并发数（环境变量 AGENT_MAX_CONCURRENT_REQUESTS）被配置成了 4，而我们一批要采 64 条 rollout——64 条轨迹被 4 条通道串行放行，这是人为瓶颈。开高之后吞吐立刻好一截。

第三层才是真瓶颈：每条轨迹是 agent 和 simulator 一来一回的 8 轮"乒乓"，轮与轮之间是严格串行的——第 3 轮必须等第 2 轮说完。加上补全长尾：64 条里 9 条 38 秒就完成了，但最后一条要 14 分钟。整个 batch 的时长由最慢的那条决定。

关于那两个看似矛盾的数字：busy-time 98% 在 agent，意思是把所有轨迹各自的"等待 agent 生成"的墙钟时间加起来，占了总等待的 98%——这是按轨迹视角算的；GPU 63% 空闲，是硬件视角——rollout 相位里 GPU 并没有被喂饱，时间散在串行等待、请求调度和推理路径本身的开销里（后来还单独查出非零 LoRA 的推理路径本身偏慢，那是 G4-3 的故事）。两个数字都对，必须联合解读：瓶颈不是算力不够，而是串行等待结构加推理路径效率。这也是为什么单纯加 GPU 没用。

一个诚实的口径提醒：从 20 分钟压到 2-3 分钟是跨了好几个阶段、换过配置和硬件逐步做到的，各改动的贡献来自不同阶段的测量，不能包装成"同一硬件上的受控基准测试"。讲的时候要分阶段说每个改动各自带来了多少。

依据：Note 021/022、throughput handoff §11

### G4-2 你们自己实现的 batched turn-boundary runner 实测慢了 2.1 倍，为什么？vLLM 的 continuous batching 和客户端攒批冲突在哪？

这是"先优化后测量"的典型教训。当时的假设是：客户端把请求攒成整齐的波次——所有 agent 同时发一轮、等全部回来、再一起发下一轮——服务端批处理会更高效。实现完之后实测，一轮从 538 秒变成 1127 秒，慢了 2.1 倍。

根因在于对 vLLM 的理解错了。vLLM 服务端做的是 token 粒度的连续批处理（continuous batching）：一个请求生成完立刻换出、新请求立刻插入，调度是流式的，根本不需要客户端帮它"攒批"。我们客户端的整齐波次不但没增益，反而引入了两个真实的损失。一是波次屏障：每一轮要等全波次里最慢的那条补全才能进下一轮，长尾被每一轮放大一次。二是破坏了相位错开：原来的流水线里 agent 生成和 simulator 生成天然错开，两张卡各忙各的；同步成波次后变成"大家一起等 agent、再一起等 simulator"，硬件利用率反而降了。

教训两条：一是先测后优化，尤其是性能优化，假设必须先用小实验验证再写代码；二是在生产级 serving 栈上，客户端批处理通常是负优化——服务端的调度器比你更懂怎么拼 batch。代码已经回滚并标记永久废弃，这个方向不会再试。

依据：throughput handoff §10、ADR-interim 20260628

### G4-3 LoRA 为什么训练省显存、serving 却可能更慢？你们那次 6 倍回归怎么定位的？

LoRA 的原理一句话：训练时冻结主模型，只学一个低秩的增量矩阵，优化器状态跟着这个低秩维度走，所以显存省一大截。这在训练侧是纯收益。serving 侧的问题在于：增量矩阵要么在推理时现场合并进主干，要么用一个专门的 LoRA 推理路径在每一层额外算一遍增量再叠加回去——后者就是慢的来源。

我们遇到的是一次 6 倍回归：吞吐量从每秒 1511 个 token 掉到 250 个。定位分三步。第一步，排除最显眼的假设：prefix cache（前缀缓存，多轮对话里重复的系统提示可以复用）失效——实测缓存命中率稳定在 94%，这个假设被证伪。第二步，跑一组"廉价修复"的矩阵实验：关掉 CUDA graph、调整分块预填充参数等等，每个配置各带来 3% 到 11% 的改善——结论是"没有廉价修复"，问题不在配置层。第三步，把根因收窄到非零 LoRA 的推理内核路径：我们用的秩是 128，走的是专门的 LoRA kernel 路径，慢就出在那里。

这里有一个诚实的边界要交代：我们没有做内核级的性能剖析（profiler 抓具体哪个 kernel 慢），所以严格说根因是"收窄到 LoRA 推理路径、并排除了所有廉价配置项"，不是"证明了某个 Triton kernel 有固有缺陷"。但工程上这已经足够支撑决策了：训练继续用 LoRA 省显存，serving 切换成合并后的全量权重——多占点显存，换回 6 倍吞吐。另外注明一下，这个实验是在两张 A6000 上做的，跟现在的 4090 环境不同，数字不能直接平移。

依据：Note 023、lora-triage handoff

### G4-4 async pipeline（k=1）为什么失败？"off-policy 一步"到底差在哪？

异步流水线的想法很美好：rollout 和训练别串行了，边采边训，硬件不闲着。我们试了允许一步滞后（k=1，意思是训练可以用上一个版本策略采的数据），结果比严格同步还慢 2 倍，而且 44% 的 rollout 被丢弃。

根因有两层。第一层是前提就不成立：异步的收益来自"训练时 GPU 闲着、可以并行采下一批"，但我们实测瓶颈本来就在 rollout 侧——训练那几十秒根本不是大头，没有空闲可填。异步解决的是一个我们不具有的问题。

第二层是语义兼容性问题，这个更隐蔽。我们 serving 用的是合并后的全量权重，服务里同一个时刻只有一份在线模型。一条 episode 采到一半，训练完成、权重更新、旧模型下线——这条采到一半的轨迹再去找旧模型，直接 404。这里的关键概念区分是：k=1 管的是"采完之后的数据，训练认不认"，管不了"采到一半旧模型消失了"。当时 LoRA 模式下异步是能跑的，因为服务允许同时挂两个 LoRA 适配器，旧策略还"活着"——这个对比让我们确认了根因就在权重版本的生命周期上。

语义层再补一个观察：其实严格同步下也不是"绝对同策略"——一次训练内部会对同一批数据跑几十次小步优化（中位数 40 到 48 次），采数据用的旧概率是固定的，训到后来策略已经漂了一段。这本来就是 PPO 式的小步漂移，业界接受。那个失败的异步实验里，漂移量大约是严格同步的 1.8 到 2 倍——这是那一次的观测值，不是普遍定律。

教训：异步改造前要先读实现源码验证语义兼容性，特别是权重版本的生命周期，不能只看算法层的 staleness 分析。

依据：Note 024/025、async handoff §4

### G4-5 训练中后期 val strict 从 0.844 掉到 0.804、grad_norm 尖峰 34.98——怎么区分"没东西可学"和"优化不稳定"？为什么局部修复全生效后 run 仍被 reject？

这两个假设的处方完全不同："没东西可学"要换数据，"优化不稳定"要修训练。判别依据是两个旁证。第一，格式失败率从零开始出现——模型开始忘记闭合标签、幻觉出用户轮次——这不是"任务太难学不动"会有的症状，是分布被甩出训练域的症状。第二，熵的尖峰和梯度范数的尖峰在时间上对齐——说明某些 batch 里损失在剧烈波动。两个证据合起来，结论是两者叠加：确实有一部分桶饱和没信号了，但训练不稳定是真实存在的问题，而且是主因。顺带说梯度守门员本身的设计：它不用固定阈值，而是取"绝对下限 2.0"和"滚动中位数的若干倍"（当前代码是 7 倍）两者中较大的那个——自适应各阶段的正常波动，而中位数对尖峰本身稳健，不会被一次爆发带歪。

再往深挖，梯度尖峰关联到一个切片（T4-R）里的特定任务族：涉事批次里真正参与损失计算的 assistant token 只有一百多到两百多个，而损失是按 token 数平均的——分母小，同样的异常分子就被放大成尖峰。这里有个要诚实交代的边界：我们最初的归因是"序列填充到 4096 加上回答太短"，后续的技术校正指出健康批次的回答长度其实也在同一量级，单看填充长度解释不了；能确认的是某个特定任务族（冒充类）产生了异常的梯度信号，而按 token 平均的分母把这个异常集中放大了。修复方案是给分母加一个下限（floor）。下限选多少也有讲究：候选的 4096 会把正常批次也压到只剩三四成，等于全局缩学习率，被拒；最后选的 2560，只兜住病态小分母，不影响正常批。

最有教育意义的是结局：所有局部修复全部生效——梯度守门员在 823 个梯度更新批次里零触发，全绿——但最终验收还是拒绝了这一轮。因为验收看的是全局指标：后期的残差提升是负 0.1087，也就是修完之后的 run 整体还是没学出净提升。教训凝练成一句：局部修复全部正确不等于全局通过，验收门必须用真实 batch 的形状做可达性推演——"就算所有修复都完美工作，目标指标数学上能达到吗"，这个问题要在跑之前回答。

依据：Note 025/026、t4r-rca handoff

### G4-6 NoHitChecker 事故后你们上了 JRA。JRA 能证明什么、不能证明什么？为什么还需要独立的语义审计？

先讲事故。有一轮 T2 探针，我们以为接的是混合判定链（NLI 加裁判），实际接到的是一个叫 NoHitChecker 的占位判定器——它永远判"没说到"。讽刺的是这批探针表面上跑完了，甚至租房办结这一类还拿了 82/96 的"好成绩"——因为这一类恰好靠关键词兜底就能蒙对，反而把事故盖住了。事故暴露的结构性问题是：当时配置审计用的是配置文件的 sha256 哈希，它绑定的是"配置文件的身份"，绑定不了"运行时实际加载的类的身份"——配置文件写的是一个类，代码里接到的是另一个类，哈希照样对得上。

JRA（运行时身份审计）就是冲这个来的：训练启动时，把判定器实际加载的类、模型快照、阈值包、裁判开关、提示词、服务端点逐字段比对，全部一致才放行。它能证明的是：此时此刻在跑的判定链路，就是你声明的那一条。

但它有一个明确的证明边界：它证明不了判定器的语义判断本身是对的。一个身份完全正确的裁判，照样可能判错——比如模型学会了裁判偏好的措辞，"说一套做一套"，裁判被合法地骗过。JRA 管不了这个，就像摄像头能证明"监考老师是本人"，证明不了"监考老师没放水"。

所以还需要第三层：盲化的独立语义审计。做法是抽一批轨迹，换一个与训练链路完全独立的评判者，在不知道训练侧结论的情况下重新判，比对两边的差异。这层审计已经预注册为 T4 的必做项——注意口径：是"计划里写死的待办"，还没执行，不能说成已经做过了。

依据：`expert-consult-p5-t3b-planning-20260809.md` §1-2、`plan030_p5_jra.py`

### G4-7 什么样的任务才有资格进 RL 的梯度面/评测面？你们的任务有效性契约是什么？

这个问题的背景是我们清退过 247 条"坏题"——事故本身怎么发现的我放在自述里讲过了，这里只讲沉淀下来的方法论：一条任务想进训练或评测，必须先通过五个问题的拷问，任何一个答不上来就出局。

第一，正确答案有可观测的证据吗？第二，这个证据用允许的工具或对话真的拿得到吗？第三，政策卡支持这个终局动作吗——政策里有没有这一条？第四，标准答案轨迹、环境状态和终局标签三者一致吗？第五，reward 真的能区分"做对"和"做错"吗？

用两个真实的反例说明这些问题不是摆设。例一：冻结状态任务的升级人工题。政策设定账户被冻结时该转人工，但工具层根本没有"查冻结状态"的接口，政策卡里也没写这条规则，simulator 也不会主动说。于是一个完美 agent——每一步都做对——在这题上必得零分。它违反的是第二、第三条：证据不可达、政策不支持。例二：冒充检测的幽灵题。设计意图是"用户冒充他人，agent 应拒绝办理"，对抗标记写了——但只写在元数据里，从来没被注入到用户开场白。于是对话里根本不存在任何对抗信号，题面让 agent 检测一个不存在的东西。它违反的是第一、第五条：没有可观测证据，reward 无从区分。

治理上的教训比反例本身更重要。其实我们早就有这些校验规则，注册表也写了——但负责校验的函数从来不运行完整的注册表，规则形同虚设。所以后来的硬措施是：有效性门改成失败即拒绝的接线方式（不通过就物理上进不了池），并且立了一条规矩——测量面的有效性门必须在任何里程碑之前验收。顺序错了，后面所有数字都是垃圾。

依据：Note 029/030/031、P5-T0 方案

---

## G5 由点扩面：RL/LLM 基础（结合项目）

### G5-1 GRPO 用组内均值当 baseline，相比 PPO 的 critic 放弃了什么、换来了什么？K=8 怎么定的？

先把两个东西说清楚。PPO 的 critic 是一个跟策略网络一起训练的"价值网络"，它的活是预测"从当前局面走下去，平均能拿多少分"，每条轨迹的实际得分减去这个预测值，就是这条轨迹比预期好多少。GRPO 把这个网络整个删掉，换成一个更朴素的基准：同一个任务采一组（我们采 8 条），拿组内平均分当基准，比平均好就是好。

换来什么？三样实在的东西。一是显存：critic 通常跟主模型一样大，我们两张 4090 本来就紧巴巴，省掉一个等大模型是能不能做的问题。二是少一个不稳定源：critic 自己也是学出来的，它学歪了会反过来带歪策略，训练曲线里很多诡异震荡都是价值网络贡献的。三是绕开一个难问题的定义权：多轮 agent 任务里"中间局面的价值"本身就很难定义——一条轨迹的价值要等 8 轮之后才知道，中途打分基本是猜。

放弃什么？也要诚实。critic 能跨任务学习——它见过成千上万条轨迹后，对"这类局面一般什么水平"有一个全局的判断，所以它能给单条轨迹一个绝对的评价。组内均值只在同一个任务内部有意义，换一批任务基准就变了。直接的代价就是我们反复提到的零方差问题：一组全对或全错，基准和每条一样，信号为零，这组就废了。critic 不会因为"同组恰好同分"而机械归零——当然它自己估计得差的时候信号照样弱，只是失效模式不同。

K=8 是算力和方差的折中。K 越大，组内均值这个基准越准（大数定律），信号质量越高；但成本近似线性涨——K 从 8 到 16，同样预算下能训的任务数少一半多（我们测算过约 54% 的成本增幅）。8 是"基准够用的最小值"附近的取值：8 条里 2 到 6 条成功就有信号，再小（比如 4）很容易整组一边倒。

依据：Note 024、power-sizing consult §3.5c

### G5-2 DAPO、GSPO、CISPO 分别改了 GRPO/PPO 训练中的哪个环节？你们为什么停在 CISPO 不再换算法？

这三个经常被一起提，但它们动的根本不是同一个环节。我按三条轴来记。

第一条轴，采样和过滤。DAPO 的招牌改动（dynamic sampling）在这一层：零方差组不直接扔，而是补采新组填进来，保证每个训练步的有效组数。它动的是"什么数据进训练"，不动损失函数。

第二条轴，重要性比率的粒度。GSPO 动这里：PPO 系的传统做法是每个 token 算一个新旧概率比率，长序列里几千个 token 级比率各自带噪声，在损失里累积放大，方差随序列长度恶化。GSPO 把重要性比率提到序列级——整条序列只算一个（按长度归一的）比率，方差来源从几千个 token 收敛到一个序列级量。它动的是"怎么度量策略变了多少"。

第三条轴，裁剪和梯度路径。CISPO 动这里：比率 detach 之后裁剪当权重用，被裁的 token 保留梯度。细节在 G3-1 讲过了。它动的是"梯度怎么流"。

所以这三个不是替代关系，是正交的旋钮，理论上可以叠加用。

为什么停在 CISPO？两个理由。一是它在我们主线上几乎免费：严格同策略下新旧策略几乎一致，裁剪几乎不触发，CISPO 的行为约等于普通策略梯度，没有引入新风险，又保留了偏移时的兜底。二是更重要的一条：我们项目的瓶颈不在算法。那时候的瓶颈是任务有效性（坏题）和测量面（判定器可信度），在这些上面换算法是打在错误的靶子上——算法换得再勤，垃圾数据训出来还是垃圾。把时间花在数据和测量上，收益大得多。

依据：Note 024/031、survey 文档

### G5-3 SFT 的交叉熵和 RL 里的 KL anchor 在优化目标上有什么区别？为什么防跑偏用 KL 而不用 CE 当惩罚项？

这题的关键是交叉熵有两个方向，不说清方向很容易把自己绕晕。

SFT 用的交叉熵，方向是"数据分布对策略"：期望在真实数据上取，让策略给真实样本的每个 token 赋高概率。它可以拆成两项：数据分布自身的熵（一个常数，跟模型无关），加上数据分布对策略的 KL 散度。所以最小化 SFT 的交叉熵，等价于最小化"数据对策略"方向的 KL——这种方向有个外号叫 mode-covering（覆盖众数）：数据里有的行为，策略都得给点概率，漏了会被重罚。这就是为什么 SFT 出来的模型说话像人——人话的各种说法它都沾一点。

RL 里的惩罚项是另一个方向："策略对参考模型"的 KL——在策略自己采出来的样本上，度量策略偏离参考模型多少，偏离就罚。它只管"别跑偏"，不管"数据里有什么"。

那能不能偷懒，把惩罚项也写成交叉熵？可以，但写出来的必然是另一个方向：策略分布对参考分布的交叉熵。这个式子拆开来是策略自身的熵加上 KL 项——注意这里多出来一个"策略自身的熵"。最小化它，等于在罚偏离的同时还顺手压策略的熵，也就是逼模型变得更"确定"、更复读——这正是我们要防的病本身。所以结论精确地说是：惩罚项必须用纯 KL，因为同方向的交叉熵里夹带了一个压熵项。答题时必须先把 CE 的方向声明清楚，不然"CE 压熵"这个结论对另一个方向根本不成立。

落回项目：KL 锚定的具体战功就是防"政务复读机"（G3-5 讲过），系数 0.08。

依据：`cross-entropy-vs-kl-diverge.md`、OPD survey §2

### G5-4 判断一个任务桶"RL 学不学得动"为什么看 pass@k 不看 pass@1？pass@k 怎么算？

pass@1 是单次成功率：采一条，成的概率。pass@k 是采 k 条，至少有一条成的概率。看 RL 学不学得动要看后者，原因是 GRPO 的信号机制：梯度来自组内的对比——组里得有成的也有败的，才知道"什么行为该鼓励、什么该抑制"。一个任务 pass@1 只有 0.16，听起来很难，但 pass@8 的意思完全不同：8 条里大概率能出一两条成的，这一两条就是学习信号的种苗。pass@1 低只说明"单兵作战能力弱"，pass@k 才回答"组内对比还有没有戏"。

pass@k 有两种算法，别混。第一种是独立同分布近似：假设每条成功率独立都是 p，那就是 1 减 (1-p) 的 k 次方。p=0.16、k=8 代进去约 0.75——意思是八成概率组内有至少一条成功。这个算法简单，但"每条独立同分布"是假设，实际有相关性时会偏乐观。第二种是有限样本的无偏估计：已经实测采了 n 条、其中 c 条成功，估计"再采 k 条至少中一条"的概率，公式是 1 减 C(n-c, k) 除以 C(n, k)——组合数，从 n-c 条失败里抽 k 条全中的概率。这个直接用观测计数、无偏，是评测报告里该用的口径；但也别把它说成免假设——它默认那 n 条样本是可交换的，rollout 之间有相关性时照样会偏乐观，只是不再额外假设"每条独立同成功率"。另外一个概念细节：pass@k 的含义是"至少一条成"，严格的"组内有对比信号"还要排除"全部成功"这种零方差情形——在低成功率区间全成的概率可以忽略，但概念上要分得清。

对应到项目里，两个"真死区"就是用这个工具判出来的：pass@k 约等于零的桶（8 条全败，连种苗都没有），和 hard-zero 平零桶（比如接了坏判定器，全组同分 0，不是任务难是信号死了）。我们的冷启动判据和可学习性分档（2/8 到 6/8 核心带）全都建立在 8 条探针的 pass 计数上。

依据：Note 007、`free_rollout_eval.py`

### G5-5 on-policy 和 off-policy 的界线在哪？staleness 怎么度量、为什么需要 importance sampling 修正？

界线一句话：采数据用的策略，和现在正在被更新的策略，是不是同一个。是同一个（严格说，是当前参数版本）就叫 on-policy（同策略）；不是就叫 off-policy（异策略）。现实中几乎没有绝对的同策略——数据采完到梯度算完之间策略多少动过，所以严格说大家都在一个谱上，差别在偏移多少。

staleness（陈旧度）就是度量这个偏移：这份数据是几个版本之前的策略采的。我们项目里 k 就是这个意思——k=0 是严格同策略，k=1 允许用一个版本前的数据。

为什么偏移了就需要修正？因为策略梯度这个算法的数学期望，是在"数据来自当前策略"的假设下推导的。数据来自旧策略，期望就有偏——你算出来的梯度方向不是当前策略的真实改进方向。修正工具叫重要性采样（importance sampling）：给每条数据（或每个 token）乘一个权重，权重是"当前策略采出它的概率"除以"旧策略采出它的概率"。概率比在 1 附近，说明两个策略差不多，不偏；偏离 1 越远，权重越极端，修正力度越大。PPO 的裁剪、CISPO 的 detach 裁剪，本质都是对这个权重做限幅——因为不限幅的话，个别 token 的权重会大到把梯度带飞，所以修正都是有截断的近似修正。

落回项目：主线严格同策略（k=0），代价是硬件利用率低、但归因最干净；异步实验（k=1）的失败故事在 G4-4 讲过；CISPO 可以理解为"重要性权重裁剪到 0 到 5 倍区间、且被裁也保留梯度"的宽容版修正。

依据：Note 024、throughput survey §3

### G5-6 你报的 +9.21pp 在统计上站得住吗？主分析为什么用 paired delta + clustered bootstrap？

先说边界：实验对照设计本身（A/B 两组怎么冻、为什么一次性看数据）我在自述里讲过了，这里只讲统计推断这一层——数字怎么算、置信区间怎么来。

第一层，比较单位是配对差（paired delta）。我们不在"RL 后的成功率"和"起点的成功率"两个独立数之间做减法，而是对同一个任务，算出"RL 后减 RL 前"的差值，再对所有任务的差值做统计。为什么要配对？因为任务之间难度差异巨大——有的任务 baseline 90%，有的 20%。不配对的话，这个难度差异会原封不动地进方差，把真实提升淹掉；配对之后，每个任务跟自己比，难度这个干扰项被消掉了。

第二层，置信区间用按族聚类的自助法（family-clustered bootstrap）。普通 bootstrap 假设样本独立同分布，但我们的任务不是：同一族的任务共享骨架和词表，一荣俱荣一损俱损，它们是相关的。按独立假设算，方差会被低估、区间会偏窄——也就是"假装比别人更确定"。聚类 bootstrap 的做法是重采样的时候整族整族地抽，保留族内相关性。另外有个叫设计效应（DEFF）的量，只在设计阶段用来粗估"相关性相当于打了多少折的样本量"，正式推断里不直接用它——而 P5 的 holdout 刻意做成一族一条，把这个问题从源头消掉了。

第三层，看数纪律：整套分析预先注册，只看一次，不许"看了一眼不理想就换个切法再看"。这是防多重比较——看二十次总有一次"显著"，那是噪声不是效果。

最后报数：S1 切片提升 9.21 个百分点，按上面这套口径算出来的族聚类置信区间是正 3.29 到正 15.13，区间整体在零以上。这就是我们敢报这个数的原因。

依据：`expert-consult-p5-power-sizing-20260806.md`、`adr-phase6-p5-t0-scope-and-gates-20260806.md` §7

---

## G6 业界动向与启发

### G6-1 业界 agentic RL 的工作里，哪几个直接改变了你们的设计？

我不背论文数字，讲机制层面的影响——哪篇工作的哪个发现，改变了我们的哪个决策。

第一个是 AReaL-SEA 系列工作的一个观察：如果用户模拟器不专门训练、直接拿现成模型当群众，RL 训练会显著退化——因为模拟器的行为分布跟真实用户差得远，agent 学到的是"怎么对付这个假群众"。这直接催生了我们的 Phase 4：单独给 simulator 做一轮监督微调，并且配了五条硬门槛——指令遵循率不低于 0.95、信息释放时机正确率不低于 0.90、人物设定一致性不低于 0.90、过早终止率不高于 0.05、无关扯话率不高于 0.05——不达标不许进训练。第二个影响同样来自这类工作：动态过滤和按方差筛任务的思想——学不动的组不要硬训。这落成了我们的零方差丢弃和可学习性分档。

第二个是 CoVe（约束验证方向）的两个思想。一是"约束模糊化"：真实用户不会第一句话就把所有字段报全——"我要取公积金"后面跟着一堆没说出口的前提。我们的应对是把"哪些信息用户一开始不说、被问到才说"显式建模成任务定义的一部分（模糊度画像加透露规则），而不是指望数据天然长这样。二是"验证器应该验证终态和关键约束，而不是验证轨迹唯一性"——只要最终状态对、关键约束满足，路径可以多样。这正是我们终局奖励的设计哲学，也解释了为什么标准答案轨迹只做校验工具、不进奖励。

依据：`方案评估报告.md`、`phase2-scenario-design.md`

### G6-2 on-policy distillation 最近很热，为什么不做你们的主线？蒸馏和 RL 在你们项目里怎么分工？

先把 on-policy distillation（同策略蒸馏）说清楚：学生模型自己 rollout，然后在一个强教师的指导下逐 token 对齐——教师告诉学生"你采的这条轨迹，每个 token 我认为该是什么分布"。它比传统蒸馏强在学生在自己的错误分布上学习，治的是 exposure bias（曝光偏差）：传统监督学习训练时看的是标准答案，推理时走的是自己生成的历史，训推分布不一致，误差会累积。

它做不了我们主线的原因很实在：我们没有合格教师。同策略蒸馏需要两样东西——学生能自己采轨迹（这个有），和一个比学生强、且懂我们环境的教师（这个没有）。外部大模型不懂我们的工具协议、看不到后台真实状态，它给的"正确答案"在我们环境里经常是错的。还有一个细节坑：教师的输出格式模板会在蒸馏时拽偏学生的格式，破坏我们严格定义的输出协议。而我们的环境里 ground truth 本来就来自可执行的仿真环境——结局对不对，环境自己知道，不需要一个教师来"认为"对不对。这种"奖励可验证"的场景，天然是 RL 的领地（业界叫 RLVR，可验证奖励的强化学习）。

所以分工是："蒸馏压缩已知，RL 探索未知"。教师能做的事（通用能力、格式）用蒸馏高效传递；环境才知道对错的事（该拒绝还是办结），用 RL 从反馈里学。

蒸馏在我们项目里还有一个具体的预案角色：防遗忘兜底。如果监控发现 RL 训练把通用指令遵循能力练没了，候选方案是自蒸馏——拿 GRPO 训练前的自己当教师（同一个 tokenizer，绕开跨模型分词不一致的坑），把能力蒸回来。这是有触发条件的选项，默认不执行。顺便一个相关共识：同策略（on-policy）的学习方式比离线式的更新更不容易遗忘，业界叫 RL's Razor——注意这个对比的准确口径是"同策略对异策略"，不是笼统的"RL 对蒸馏"，同策略蒸馏本身也占这个便宜。

依据：`on-policy-distillation-applied-to-this-project.md`、OPD survey §1.3/§5

### G6-3 业界解 RL rollout 吞吐的路线有哪几类？async 的立场分裂你怎么看？

业界的路线我归成四类。第一类从算法侧减轮数：比如只在不确定性高的轮次做 RL，其余轮次免训练——代表是 PivotRL 这类思路。第二类多任务复用：好几个训练任务共享采样集群，谁的 GPU 闲了谁去采——代表 RollMux，前提是你得有好几个并发任务。第三类异步流水线：rollout 和训练解耦并行跑——代表 AReaL、StreamRL、Laminar 这些。第四类框架工程：不动算法，把参数同步、显存放置、调度做到极致——verl、ReaLHF 是这个路线。

我们的现实约束是两张 4090、单任务。四条路线筛一遍：多任务复用没有前提；减轮数要改算法和奖励归因，代价大；异步的前提（训练时有空闲可填）在我们这不成立，实测失败了（G4-4 的故事）。所以真正有效的只有第四类加客户端并发调优——这也和我们实际的优化路径吻合。

关于异步的立场分裂，业界确实吵：AReaL 的实验说 staleness（数据陈旧）在一定范围内对收敛无伤，RollMux 的实验说它确实伤。我的看法是这个分裂的根源是任务敏感度不同——不同任务的奖励结构和策略漂移速度不一样，有的任务吃点陈旧数据没事，有的（比如我们这种终局动作要求很硬、归因要求很强的）就敏感。所以这不是"谁对谁错"，是"你的任务站在谱系的哪一端"。我们项目的安全约束硬、归因要求强，所以主线选严格同步；切换条件也事先写明了：什么时候 rollout 不再是瓶颈、且权重版本生命周期问题有解，才重新评估异步。

依据：`rl-rollout-throughput-survey.md`

### G6-4 你的 reward 走规则+verifier 可验证路线，和 reward model / LLM-as-judge 路线怎么分工？judge 怎么防成为单点？

两条路线的分工原则就一句：能程序化验证的，绝不用模型判。终局状态对不对、终局动作选没选对、轮数超没超——这些都有程序化的判定方法，结果精确、免费、不可能被"说服"。在这种场景里引入奖励模型（reward model）是不必要的近似：你明明有标准答案，却训练一个模型去猜标准答案。只有"话说到位没有"这种天然语义化的判断，规则做不了，才交给判定器——而且判定器也只处理本地 NLI 和关键词兜底之后的疑难残余，暴露面被压到最小。

防单点是三层防线，一层管一种失效模式。第一层就是上面说的结构：可验证规则优先，judge 的管辖范围天生就小——它判错的代价被限制在 disclosure 这一个分量里。第二层管"judge 被掉包"：JRA 运行时身份审计，保证实际在跑的就是你声明的那套判定链路（NoHitChecker 事故换来的教训，G4-6 展开过）。第三层管"judge 是本人但在放水/被骗"：盲化的独立语义审计——换一个独立评判者重新判一批样本，比对两边差异。这防的是模型学会裁判偏好的措辞来骗分（reward hacking 的裁判版）。

还有一个更细的设计也算防单点的一部分：判定链的方向不对称（G1-5）。"该说的说到了"要复核、"不该说的没说"直接放行——两个方向的误判代价不同，所以信任程度不同。单点的本质不是"只有一个组件"，而是"一个组件的错误没人兜底"，我们的每一层兜底都是按误判代价专门设计的。

依据：`phase5-reward-pipeline.md`、Note 030/031

---

## G7 代码实现卡

### G7-1 手写你们实际优化的 loss：给定同组 K 条 rollout 的 reward 和 per-token 新旧 logprob，写出 advantage 和 token-level loss，并指出它和标准 PPO 差在哪几行。

分两步写。第一步算组内优势分（伪代码）：

```python
# rewards: 同组 K=8 条 rollout 的总分
mean = rewards.mean()
std = rewards.std()
advantage = (rewards - mean) / (std + 1e-6)    # 每条轨迹一个标量
# 注意：ART 实现里这个稳定常数是 1e-6；训练器配置里另有一个 1e-12
# 是判定"零方差组"用的，两个 epsilon 不是一回事，别混
# 组内全同分（std≈0）的组整组丢弃，不进 loss
```

第二步是逐 token 的损失。对每条轨迹里的每个 assistant token：

```python
ratio = (new_logprob - old_logprob).exp()      # 新旧概率比率
weight = ratio.detach().clamp(0.0, 5.0)        # 关键行：detach 后裁剪成权重
# 默认 epsilon=1.0、epsilon_high=4.0 → 区间 [1-1, 1+4] = [0, 5]
# （上限是 1 加 4 等于 5，不是 4；监控里 prob_ratio_max>4 的暂停阈值是另一回事）
loss = -(weight * advantage * new_logprob)     # 梯度只从 new_logprob 流
# 最后按 assistant token 的 mask 求平均
# （生产实现里还有轨迹级权重和 KL 惩罚折算进优势分，上面是核心形式）
```

和标准 PPO 差在三行。第一，PPO 的 ratio 不 detach，它带着梯度进目标函数；我们这里 detach 了，比率纯粹是个权重标量。第二，PPO 的目标是 min(ratio·A, clip(ratio)·A)——两个分支取小，进入被裁分支的 token 梯度为零；我们这里没有 min 结构，被裁的 token 只是权重压到上限 5（1 加 epsilon_high 4），梯度照流。第三，裁剪范围量级不同：PPO 习惯在 1 两侧各留 0.2，我们的区间是 0 到 5——因为严格同策略下比率本来就贴着 1，裁剪是兜底不是常态。

依据：`ART/src/art/loss.py:188`、`tokenize.py:264-275`

### G7-2 KL 散度的三种常用估计子（k1/k2/k3）写一下，各有什么偏差/方差性质？

先冻结符号：令 x = 当前策略的 log 概率减参考策略的 log 概率（对同一个 token），样本从当前策略采。要估的是"当前策略对参考策略"的 KL。

k1 = x。直接用对数比率当估计。性质：数学期望上无偏，但单次取值可正可负，方差很大——一条序列的 k1 估计跳来跳去。

k2 = x² / 2。对数比率平方再减半。性质：永远非负，看着很稳，但它是有偏的——期望不等于真 KL，只是 KL 的一个二阶近似，偏离大的时候偏得明显。

k3 = exp(-x) - 1 + x，也就是 概率比率的倒数减一再减对数。性质：无偏而且非负，是三个里性质最好的；代价是里面有指数，x 很负的时候 exp(-x) 会爆，数值上要处理。

落回项目：KL 惩罚项用的是 k1 家族的逐 token 对数差（配系数 0.08 折算进优势分）；漂移监控同时看两类指标——带符号的 k1 均值（可正可负，指示漂移方向）和绝对对数差、比率分位数这类非负指标（指示漂移幅度）。不存在"监控必须非负"的硬性约束，按要回答的问题选估计量。

依据：`cross-entropy-vs-kl-diverge.md`、`patches/art/phase6-cispo-drift-telemetry.patch`

### G7-3 多轮 agent 轨迹的 loss mask 怎么构造？你们项目里这个 mask 依赖什么容易踩的坑？

loss mask 是一条和 token 序列等长的 0/1 数组，1 表示"这个 token 参与损失计算"，0 表示"只当上下文，不算它的账"。构造原则一句话：只有 assistant 说的话才计损失——因为损失的意义是"让模型为它的决策负责"，而用户的话、工具的返回不是模型的决策，模型不该为它们担梯度。

实现上是按轮构造：拼完整条多轮序列时，逐轮记录每个 assistant 消息的 token 起止区间，区间内置 1，其余（系统提示、用户轮、工具观察）全置 0。

我们踩过的坑有两个，都很隐蔽。第一个：rollout 拿到的 assistant 消息必须保留原始的 Choice 对象（推理引擎返回的完整结构），因为里面带着逐 token 的 log 概率——这是算新旧比率必需的。如果中途把消息转成字符串再重建对象，log 概率就丢了，这条轨迹会被 mask 判定为"没有可训练的 assistant token"，静默地从训练里消失——不报错，只是数据偷偷变少。第二个坑：工具返回的消息，不同推理引擎用的角色名不一样（有的叫 tool、有的叫 observation 或其它），送进聊天模板前要统一归一成模板认识的角色名，否则模板拼序列时会把工具观察整段丢掉，agent 等于"看不见环境返回"，训出来的行为完全失真。

依据：`trajectory_adapter.py`、`rollout.py`

### G7-4 走查你们的 reward 结算：一个 episode 结束后从轨迹到 R_total 的完整数据流是什么？

按数据流动的顺序走一遍，不背公式。

episode 一结束，先过配置绑定校验：本次运行加载的 reward 配置要和冻结配置做完整绑定校验（规范化后的配置内容、绑定标识、判定器包逐层对，不只是比一个哈希值），不一致直接失败拒绝——这是 NoHitChecker 事故之后立的规矩，宁可不跑也不跑错。

然后按顺序算几个分量（代码里是依次调用，不是并行管线）。第一个是状态分：把轨迹结束时的真实数据库状态导出来，跟任务定义的目标状态做比对。比对不是简单全等，而是按一份比对规范逐字段来——有的字段要求精确相等，有的属于某个集合就行，运行时注入的影子表要先剥掉。第二个是终局动作分：取 agent 自报的终局动作标签，跟轨迹里最后一个结构化的动作记录双重核对，两边一致才认；记录缺失或解析异常，按 0 处理，绝不反推。然后完成度分 = 状态分乘终局动作分，这就是那个门控。

第三个是告知分：该说的槽位逐条过混合判定链（本地 NLI、关键词兜底、大模型裁判三层），"不该说"的负面槽位一旦泄露，一票否决整个告知分。第四个是效率罚：实际轮数对比目标轮数——目标值用的是数据冻结期回放统计出来的中位数，不是拍的。

最后过硬门：轨迹里如果触发过红线违规（调了不该调的工具之类），总分直接归零，前面的分量全作废。没触红线，才把各分量按权重加总成 R_total。整条链路每一步都有落盘的审计记录，出了问题可以逐分量回查。

依据：`reward/aggregate.py`、`reward/terminal.py`、`reward/complete.py`

### G7-5 你们的输出协议（envelope）和训推一致性怎么保证？chat template 上踩过什么坑？

输出协议是我们自定义的一层信封：模型的每轮输出必须包在 分析 和 动作 两个标签里（尖括号加英文标签名），分析段写推理，动作段写结构化的下一步操作。没用基座模型（Qwen3）自带的思考通道，原因是我们自己的信封已经承担了思考区的职责，再开原生通道是冗余，而且两个思考区并存会破坏格式契约——模型可能把内容写进错误的那个。解析器全库只有一个实现，解析失败就是失败，没有静默兜底。

训推一致性指的是：训练时拼 prompt 用的模板，必须和推理时 serving 栈用的模板逐 token 一致。我们踩的坑链条是：训练框架（LLaMA-Factory）里选的模板名叫 qwen，推理侧用的是 Qwen3 自带的官方 jinja 模板——两个都叫"qwen"，但实际产物有细微差异。用逐 token 对比工具定位到两处：一是默认系统提示的有无，二是最后一轮前面会不会被注入一个空的思考块。最阴的一个发现是模板参数 enable_thinking=False 的语义不是"关掉思考"，而是"插入一个空的思考块"——这个参数按字面理解去配置，硬违规率实测从 0 飙到 0.6875。

修复方式是手写一份跟训练侧逐 token 等价的 jinja 模板给推理侧用，然后把"逐 token 对比必须完全一致"做成一道上线门。沉淀下来的原则一句话：约束我们的是训推一致性，不是模板叫什么名字——名字是标签，字节才是契约。

依据：`adr-phase3-chat-template-qwen-not-qwen3.md`、Note 011/012、`verifier/format.py`

---

## 附：答案撰写自查

- [x] 每题末尾标注依据文件
- [x] "计划/预注册"与"已执行"区分（G3-3、G4-6 语义审计、G6-2 自蒸馏预案均已标注）
- [x] 历史设计标明历史（G3-3 的 Step-2 配比采样器）
- [x] 黑话控制：所有术语首次出现配大白话解释
- [x] 两位专家评审（新 tab w1:tM、新上下文）第一轮完成：ans-ds（3 阻塞 + 10 建议）、ans-gpt2（3 阻塞 + 18 建议）
- [x] 阻塞项修复：G1-2 R_terminal 二值；G7-1/G5-5 裁剪区间 [0,5]；G7-1 稳定常数 1e-6（1e-12 是零方差过滤的）；G3-4 拆清 pur×FWR（同配方无难度干预）与 pur×Esc（DC-23 零供给，已修复已确认）
- [x] 非阻塞采纳：G1-6 hard 桶含信封/格式失败、G1-8 权重动机改为文档版、G2-3 泄漏口径与 P_turns 间接通道、G2-4 退役构成、G2-5 动作映射、G3-2 canary 口径、G3-3 点名 lrq×Esc、G3-6 停训清单改代表式、G4-1 全名与归因、G4-5 根因降格+823 批次+守门员机制、G4-6 点名 rent Finish、G5-1/5-2/5-4 表述收窄、G6-1 真实五门槛、G6-2 Razor 口径、G7-2 监控落点、G7-4 顺序与绑定校验
- [x] 两位专家收敛确认通过（收敛轮共同抓到 G7-1 一处"上限 4"残留，修复后双双同意定稿）


